# Curated third-party targets (wrappers, not forks)

Each directory holds a wrapper: upstream image pinned by digest, orientation
manifest, and review record. No upstream code is vendored.

Status: `juice-shop` live · `crAPI` queued (see docs/curated-review.md).
